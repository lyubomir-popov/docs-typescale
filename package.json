{
  "name": "docs-typescale",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
            "dev": "concurrently \"node scripts/watch.js\" \"live-server --port=3000 --open=/demo-baseline.html --entry-file=demo-baseline.html --watch=dist,src,demo-baseline.html\"",
        "watch": "node scripts/watch.js",
        "build": "node scripts/watch.js --build-only",
        "generate": "npx @lyubomir-popov/baseline-nudge-generator generate config/typography-config.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@lyubomir-popov/baseline-nudge-generator": "^1.3.1",
    "chokidar": "^4.0.3",
    "concurrently": "^9.2.0",
    "live-server": "^1.2.2",
    "sass": "^1.89.2",
    "vanilla-framework": "^4.26.0"
  }
}
